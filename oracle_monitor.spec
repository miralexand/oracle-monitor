# -*- mode: python ; coding: utf-8 -*-
"""PyInstaller 打包配置：生成单文件 dist/oracle_monitor.exe。

用法：pyinstaller --clean --noconfirm oracle_monitor.spec
"""

from PyInstaller.utils.hooks import collect_all

datas = [("config.sample.ini", ".")]
binaries = []
hiddenimports = []

for package in ("oracledb", "cryptography", "cffi", "tzdata"):
    package_datas, package_binaries, package_hiddenimports = collect_all(package)
    datas += package_datas
    binaries += package_binaries
    hiddenimports += package_hiddenimports

a = Analysis(
    ["monitor.py"],
    pathex=[],
    binaries=binaries,
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name="oracle_monitor",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
